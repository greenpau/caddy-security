interface:
  display_name: "Configuration OAuth Applications"
  short_description: "Configure named OAuth client registrations"
  default_prompt: "Use $configuration-oauth-applications to configure named OAuth applications with private provisioning, durable credentials, and portal OIDC providers in caddy-security."
